// add this import
import SmiskiModal from './SmiskiModal';

// ...inside the component:
const [shopOpen, setShopOpen] = useState(false);

// replace the coin area with a button that opens the modal:
<button
  className="coin-btn"
  type="button"
  onClick={() => setShopOpen(true)}
  aria-label="Coin button"
  title="Coins"
>
  <img className="imgicon" src={coinImg} alt="Coins" />
  <span>0</span>
</button>

// ...at the bottom of the return (after the timer card), render the modal:
<SmiskiModal open={shopOpen} onClose={() => setShopOpen(false)} />
